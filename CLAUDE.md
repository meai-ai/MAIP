# MAIP Project Instructions

## Git Commit Rules

- Always use `lvfangmin <lvfangmin@gmail.com>` as both author and committer
- Use `--author="lvfangmin <lvfangmin@gmail.com>"` on every `git commit`
- Never add `Co-Authored-By` lines
- Repo-level git config is already set (`git config user.name` / `user.email`)
